APP_NAME=Laravel
APP_ENV=production
APP_KEY=base64:0iFnNv82QhmekEU13uUc8RyL0zynDeUJJcRv7BUfLys=
APP_DEBUG=false
APP_URL=https://portfolio-anik-azmain.onrender.com

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=info

# Database (Render uses DATABASE_URL, so you should parse it in config/database.php if not already)
DB_CONNECTION=mysql
DB_HOST=${DB_HOST}
DB_PORT=${DB_PORT}
DB_DATABASE=${DB_NAME}
DB_USERNAME=${DB_USER}
DB_PASSWORD=${DB_PASSWORD}

# Sessions
SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

# Cache/Queue
CACHE_STORE=file
QUEUE_CONNECTION=database
BROADCAST_DRIVER=log
FILESYSTEM_DISK=local

# Redis (optional – Render Redis add-on provides REDIS_URL)
REDIS_CLIENT=phpredis
REDIS_HOST=${REDIS_HOST:-127.0.0.1}
REDIS_PASSWORD=${REDIS_PASSWORD}
REDIS_PORT=${REDIS_PORT:-6379}

# Mail (Mailtrap for testing, you can replace with Gmail/SMTP if needed)
MAIL_MAILER=smtp
MAIL_HOST=sandbox.smtp.mailtrap.io
MAIL_PORT=2525
MAIL_USERNAME=4472b7ff801f41
MAIL_PASSWORD=2f0c57cb2ea3d9
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=azminanik@gmail.com
MAIL_FROM_NAME="Portfolio"

# AWS (optional – if you use S3 storage)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# Vite build
VITE_APP_NAME="${APP_NAME}"
